services:
  php:
    image: php:8.2-alpine
    volumes:
      - .:/var/www/html
  composer:
    image: composer:2.9.8
    command: ["composer", "install", "--ignore-platform-req=ext-*"]
    volumes:
      - .:/app